describe('Data Filter', function() {

  beforeEach(module('DateControllers'));

  describe('Data interval filter', function() {

    it('filter between startdate1 and endDate1',
        inject(function(spotIntervalFilter) {
      //expect(checkmarkFilter(true)).toBe('\u2713');
    }));
  });
});